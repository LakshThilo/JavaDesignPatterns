package lak.designPatterns.behavioral.commandPattern.viewingDocumentEmpl;

// Next I need to create an interface to represent the commands. and need to create the concrete implementations of this interface.
public interface Command {

    public void execute();
}
